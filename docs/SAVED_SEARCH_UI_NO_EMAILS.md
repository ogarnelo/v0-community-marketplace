# No-email guardrail

The saved-search UI does not send notifications yet.

It stores demand only.

Email/push delivery should be introduced only after:

- matching rules are defined,
- duplicate suppression is implemented,
- user unsubscribe/preferences are available,
- email copy is reviewed.
