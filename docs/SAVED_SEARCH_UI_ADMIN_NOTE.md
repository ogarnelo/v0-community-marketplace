# Admin note: saved search UI

The UI writes to `public.saved_searches`, which is summarized by `public.saved_search_demand_summary`.

Admin follow-up:

- show saved search counts in demand intelligence,
- prioritize zero-result searches,
- identify repeated ISBN demand,
- use school/category/course clusters for supply campaigns.
